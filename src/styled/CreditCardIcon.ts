import styled from 'styled-components';

export const IconWrapper = styled.div`
    position: absolute;
    top: 8px;
    right: 5px;
`;

export const Icon = styled.img`
    display: block;

    height: 24px;
`;
